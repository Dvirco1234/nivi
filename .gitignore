.build/
build/
dist/
.DS_Store

# Build products of whisper.cpp, not source. `make vendor` recreates both.
vendor/lib/
Sources/CWhisper/include/

# Local agent config and agent run artifacts. They are scratch work, not part of
# the app, so they are kept out of this repo and backed up separately. No
# trailing slash: on this Mac both are symlinks, and a trailing slash only
# matches a real directory.
.claude
.superpowers
